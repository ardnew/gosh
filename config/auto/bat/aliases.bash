#!/bin/bash

# use a simpler formatting style
alias bats='bat --style=header,rule,snip --decorations=auto --color=auto --paging=never'

# language-specific bat aliases
alias bat.sh='bat --language=sh'

